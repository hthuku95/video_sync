[package]
name = "check_session"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "check_session"
path = "check_session.rs"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
sqlx = { version = "0.7", features = ["runtime-tokio-rustls", "postgres", "chrono"] }
dotenv = "0.15"